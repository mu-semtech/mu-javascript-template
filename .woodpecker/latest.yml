steps:
  build-latest:
    image: woodpeckerci/plugin-docker-buildx
    settings:
      repo: ${CI_REPO}
      tags: latest
      platforms: linux/amd64
    secrets: [ docker_username, docker_password ]
when:
  branch: master
  event: push
